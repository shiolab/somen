
<div class="row">
  <div class="col-lg-12">
    <div class="pull-right">
      <p>
        <button type="button" class="btn btn-primary" onclick="location.href='<%= new_organization_path %>'">新規組織</button>
      </p>
    </div>
  </div>
</div>

<div class="row">
  <div class="col-md-12">
    <% @organizations.each do |organization| %>
    <div class="panel panel-primary rounded">
      <div class="panel-body">
        <div class="row">
          <div class="col-md-10"><strong><%= link_to organization.name, "/organizations/#{organization.id}/mailforms" %></strong></div>
          <div class="col-md-2"><%= link_to '編集', edit_organization_path(organization), class: "btn btn-success btn-xs" %></div>
<!--
          <div class="col-md-1"><%#= link_to '編集', edit_organization_path(organization) %></div>
          <div class="col-md-1"><%#= link_to '削除', organization, method: :delete, data: { confirm: 'Are you sure?' } %></div>
-->
        </div>
      </div>
    </div>
    <% end %>
  </div>
</div>
